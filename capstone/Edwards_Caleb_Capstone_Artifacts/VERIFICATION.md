CATEGORY: COWORK-ONLY — working file. Decides what Stages 1–6 reuse vs. rebuild. Never submitted.

# VERIFICATION — Stage 0

Every artifact in `outputs\` got exactly the one check named for it, nothing more.
Nothing was repaired. All checks were run against the raw CSV or against a copy of
`part_d.sqlite`, not against the summary documents.

## Verdicts

| # | Artifact | Stated check | Verdict | Evidence |
|---|---|---|---|---|
| 1 | `CLEANING.md` | Documents exclusions, nulls, raw→clean row reconciliation? | **PASS** | Steps 1–9, each with "Rows before / Rows after". Explicit "No exclusions applied" section naming the four filtering decisions deferred to `QUESTIONS.md`. Per-column empty-string table for all 8 suppression-bearing columns. Reconciles 390,473 in → 390,473 out. Five named data limitations. |
| 2 | `CAPSTONE_AGGREGATES.xlsx` | Totals tie to 390,473 rows and $2.74B? | **PASS** | Summed the cost and claim columns on all four rollup sheets. Every sheet totals **$2,737,455,388.61** and **16,573,710** claims. Group counts: By_Prescriber 19,390 · By_Specialty 97 · By_Generic_Drug 1,177 · By_City 226 — each matches the verified distinct-entity count, so each sheet is a complete partition of the 390,473 rows. |
| 3 | `part_d.sqlite` | Table row count equals 390,473? | **PASS** | One table, `part_d`. `COUNT(*) = 390473`. `SUM(Tot_Drug_Cst) = 2,737,455,388.61` and `SUM(Tot_Clms) = 16,573,710`, both tying to the CSV. |
| 4 | `part_d_analysis.ipynb` | Runs top-to-bottom without error? | **PASS** | Executed clean-kernel via `nbconvert --execute` against the raw CSV. Exit 0. 18/18 code cells ran, 0 error outputs. Wrote `outliers.csv` (18,940 data rows), `regression_output.csv`, `PY_RESULTS.md`, `PY_FLAGS.md`. |
| 5 | `SQL_RESULTS.md` / `part_d_profiling.sql` | Queries run as written, results match? | **PASS** | All **35/35** statements executed against `part_d.sqlite` with **0 errors**. Each statement's inline `-- Sample result:` block was parsed and compared against the live result row-by-row: **35/35 matched**, no tolerance needed beyond float representation. |
| 6 | `regression_output.csv` | Reproducible from the notebook, not hand-entered? | **PASS (reproducible) — but see flag** | Re-executing the notebook regenerated the file **byte-identical** (MD5 `e090c233…` both). Not hand-entered. **Flag:** the method — multivariate OLS on categorical dummies with R² / adj-R² / incremental R² — is outside the curriculum (CALIBRATION.md §4, B1). Reproducible ≠ defensible in a live viva. |
| 7 | `charts\` | How many, and do underlying numbers tie? | **PASS (5 charts) — but see flag** | 5 PNGs. `make_charts.py` re-ran and regenerated all five plus a **byte-identical** `CHART_FLAGS.md`. Inputs are `outliers.csv` + `regression_output.csv` (both reproduced from the notebook in check 4) and the `By_Generic_Drug` / `By_City` sheets of `CAPSTONE_AGGREGATES.xlsx` (tied in check 2). Numbers tie. **Flag:** chart types (Pareto with cumulative line, `ax.barh`, `ax.fill_between`, log axis, raw `ax.boxplot`) are outside the curriculum (CALIBRATION.md §4, B4–B5). |
| 8a | `PY_RESULTS.md` | Numbers traceable to a script? | **PASS** | Written by `part_d_analysis.ipynb`. Regenerated **byte-identical** on re-execution (MD5 `9d9a5166…`). Same for the companion `PY_FLAGS.md` (MD5 `2e5b93d9…`). |
| 8b | `STATS_OUTPUT.md` | Numbers traceable to a script? | **INCONCLUSIVE** | The numbers are **correct** — spot-checked 390,473 / 19,390 / 1,177 / $2,737,455,388.61 / 229,912 blank `Tot_Benes` against `part_d.sqlite`, all present and all matching. But **no script produced it.** `RUN_LOG.md` §3–§7 documents the pandas code that generated it, run in a scratch directory that was not kept. The file cannot be re-executed. Traceable to documented code, not to a runnable artifact. |

**No FAIL. No ABSENT.** One INCONCLUSIVE (8b).

## Artifacts present in `outputs\` but not on the Stage 0 check list

Listed for completeness; no verdict was assigned because no check was specified.

| Artifact | Note |
|---|---|
| `CAPSTONE_PROFILE.xlsx` | Column-level profiling workbook (9 data sheets). Cardinalities on its index sheet match the verified entity counts. |
| `CITY_REFERENCE.xlsx` | 226-city reference plus a Notes sheet. Supports the city spelling-variant question in `QUESTIONS.md`. |
| `outliers.csv` | 18,940 rows. Regenerated byte-for-byte by the notebook in check 4. |
| `PREGAME_RESEARCH.md` | 52 KB of source research. Reference only. |
| `QUESTIONS.md` | 14 open questions, Q1–Q14, all still unresolved. Stage 0 resolved none and raised none. |
| `SQL_FLAGS.md`, `PY_FLAGS.md`, `CHART_FLAGS.md` | Data-quality and judgment notes companion to their result files. |
| `load_part_d.py` | Loader for `part_d.sqlite`. Consistent with check 3. |
| `RUN_LOG.md` | Chronological record of the 2026-08-10 run. The evidence base for verdict 8b. |

## What this means for Stages 1–6

The prior work is in materially better shape than "reference only" implied. Every
number in it reconciles to the raw file, and every computational artifact except
`STATS_OUTPUT.md` re-runs and reproduces itself exactly.

The real gap is **not accuracy — it is curriculum fit.** Two PASS artifacts
(`regression_output.csv`, `charts\`) are correct but built with techniques never
taught in the bootcamp, which the calibration rule forbids and which Caleb would have
to defend live. Those get rebuilt on taught methods, not because they are wrong.
